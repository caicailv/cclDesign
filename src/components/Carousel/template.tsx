import Carousel from './index'
import { Link } from 'react-router-dom'
import {routetList} from '../../router'

export default function template() {
  return (
    <div style={{ background: 'red' }}>
      <Carousel />
    </div>
  )
}
